// Supabase types removed — not needed when using local mock state.

export type Json = string | number | boolean | null | { [key: string]: Json | undefined } | Json[];

export type Database = { __REMOVED?: true };
